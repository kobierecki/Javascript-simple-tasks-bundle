//ROZNICA LICZB 

function returnDifference(num){
    var number = num;
    var difference = number - 13;
    return Math.abs(difference);
}

console.log(returnDifference(24));
console.log(returnDifference(10));